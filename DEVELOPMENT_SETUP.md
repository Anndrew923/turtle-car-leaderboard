# 龜車排行榜 App 開發環境設置指南

## 🚀 快速開始

### 方法一：使用便捷腳本（推薦）

1. **雙擊 `setup-env.bat`** - 自動設置環境
2. **雙擊 `start-dev.bat`** - 啟動開發伺服器

### 方法二：手動設置

1. **開啟命令提示字元 (CMD)**
2. **切換到專案目錄**：
   ```cmd
   cd "C:\Users\i\Dropbox\龜車排行榜\turtle-car-app"
   ```
3. **安裝依賴**：
   ```cmd
   npm install
   ```
4. **啟動開發伺服器**：
   ```cmd
   npm run dev
   ```

## 📋 環境要求

- **Node.js**: 16.0 或更高版本
- **npm**: 7.0 或更高版本
- **瀏覽器**: Chrome, Firefox, Safari, Edge (現代瀏覽器)

## 🔧 可用的腳本

### 環境設置腳本

- `setup-env.bat` - 完整的環境設置和驗證
- `install-deps.bat` - 僅安裝依賴套件

### 開發腳本

- `start-dev.bat` - 啟動開發伺服器
- `test-build.bat` - 測試構建

### Git 腳本

- `final-vite-fix.bat` - 提交 Vite 修復
- `ultimate-fix.bat` - 提交最終修復

## 🌐 訪問應用程式

開發伺服器啟動後，在瀏覽器中訪問：

- **本地地址**: http://localhost:3000
- **網路地址**: 會顯示在終端機中

## 🛠️ 開發指令

```bash
# 安裝依賴
npm install

# 啟動開發伺服器
npm run dev

# 構建專案
npm run build

# 預覽構建結果
npm run preview

# 檢查 TypeScript
npx tsc --noEmit

# 代碼檢查
npm run lint
```

## 🔍 故障排除

### 問題 1: PowerShell 編碼錯誤

**解決方案**: 使用 CMD 而不是 PowerShell

### 問題 2: 找不到 package.json

**解決方案**: 確保在正確的目錄中執行指令

### 問題 3: 依賴安裝失敗

**解決方案**:

1. 檢查網路連接
2. 清除 npm 快取：`npm cache clean --force`
3. 刪除 node_modules 和 package-lock.json，重新安裝

### 問題 4: 開發伺服器無法啟動

**解決方案**:

1. 檢查端口 3000 是否被占用
2. 嘗試使用其他端口：`npm run dev -- --port 3001`

## 📁 專案結構

```
turtle-car-app/
├── src/                    # 原始碼
│   ├── components/         # React 組件
│   ├── contexts/          # React Context
│   ├── pages/             # 頁面組件
│   ├── services/          # API 服務
│   └── types/             # TypeScript 類型
├── public/                # 靜態資源
├── package.json           # 專案配置
├── vite.config.ts         # Vite 配置
├── tsconfig.json          # TypeScript 配置
└── tailwind.config.js     # Tailwind CSS 配置
```

## 🎯 下一步

1. 啟動開發伺服器
2. 在瀏覽器中查看應用程式
3. 開始開發新功能
4. 使用 Git 提交變更

## 📞 需要幫助？

如果遇到問題，請檢查：

1. Node.js 版本是否符合要求
2. 是否在正確的目錄中執行指令
3. 網路連接是否正常
4. 是否有防火牆阻擋端口 3000

